{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities. Core defaults include event listen/unlisten/emit — required for the pod-shell output stream and the sidecar lifecycle events (the frontend subscribes via @tauri-apps/api/event's listen, which is ACL-gated unlike app-defined commands).",
  "windows": ["*"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-open",
      "allow": [{ "url": "https://*" }, { "url": "http://*" }]
    }
  ]
}
