# Bruno Import and Reorder (recreated stub)

## Status

`Archived` (superseded)

## Why this stub exists

Cited from `docs/features/active/post-migration-ux-review/index.md` as an in-progress companion
covering Bruno collection import/export and collection-tree reordering. The original document is
missing from this repository state. Recreated as a minimal stub so the link resolves.

## What this doc covered (inferred from citing docs, and current code)

This work is superseded by `docs/features/active/api-client-git-completion/`, which found the
original Bruno import/export buttons in the Git panel were non-functional ("fake") and removed
them entirely rather than completing them, replacing the feature area with real Git-based
collection linking instead. There is no remaining Bruno-format import/export work tracked anywhere
in this repository as of the `tauri-react-primary-tool` production-readiness review.

## See also

- `docs/features/active/api-client-git-completion/`
