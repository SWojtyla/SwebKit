# AKS Migration Fixes (recreated stub)

## Status

`Archived`

## Why this stub exists

Cited from `docs/features/active/post-migration-ux-review/*` and
`docs/features/active/monitoring-rebuild/index.md` as an in-progress companion covering AKS-specific
migration bugs (MAUI → Tauri/React), but the original document is missing from this repository
state. Recreated as a minimal stub so those links resolve.

## What this doc covered (inferred from citing docs)

Fixes for AKS-area regressions found during the MAUI-to-React migration, tracked separately from
`post-migration-ux-review`'s broader UX findings so the two efforts wouldn't duplicate work. Its
current status and any remaining items should be re-verified against the current codebase — see
`docs/features/active/tauri-react-primary-tool/production-readiness-review.md` for a fresh,
code-verified AKS gap analysis (§4, feature-parity gap table) that supersedes anything this doc
might still have open.

## See also

- `docs/features/active/tauri-react-primary-tool/production-readiness-review.md`
