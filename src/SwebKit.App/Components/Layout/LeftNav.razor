<nav class="left-nav" aria-label="Main navigation">
    <div class="nav-section">
        <NavItem NavIcon="@(new Icons.Regular.Size24.Home())" Label="Dashboard" Area="dashboard"
                 IsExpanded="IsExpanded" CurrentArea="CurrentArea" OnNavigate="OnNavigate" />
        <NavItem NavIcon="@(new Icons.Regular.Size24.ArrowSwap())" Label="Service Bus" Area="service-bus"
                 IsExpanded="IsExpanded" CurrentArea="CurrentArea"        OnNavigate="OnNavigate" />
        <NavItem NavIcon="@(new Icons.Regular.Size24.CloudCube())" Label="AKS" Area="aks" IsExpanded="IsExpanded"
                 CurrentArea="CurrentArea"                OnNavigate="OnNavigate" />
        <NavItem NavIcon="@(new Icons.Regular.Size24.Database())" Label="Redis" Area="redis" IsExpanded="IsExpanded"
                 CurrentArea="CurrentArea"        OnNavigate="OnNavigate" />
        <NavItem NavIcon="@(new Icons.Regular.Size24.FolderOpen())" Label="Storage" Area="storage"
                 IsExpanded="IsExpanded" CurrentArea="CurrentArea"             OnNavigate="OnNavigate" />
        <NavItem NavIcon="@(new Icons.Regular.Size24.Rocket())" Label="Pipelines" Area="pipelines" IsExpanded="IsExpanded"
                 CurrentArea="CurrentArea"    OnNavigate="OnNavigate" />
        <NavItem NavIcon="@(new Icons.Regular.Size24.DataTrending())" Label="Observability" Area="observability"
                 IsExpanded="IsExpanded" CurrentArea="CurrentArea" OnNavigate="OnNavigate" />
    </div>

    <div class="nav-section" style="margin-top: auto; border-top: 1px solid var(--color-border);">
        <NavItem NavIcon="@(new Icons.Regular.Size24.Settings())" Label="Settings" Area="settings"
                 IsExpanded="IsExpanded" CurrentArea="CurrentArea"            OnNavigate="OnNavigate" />
    </div>
</nav>

@code {
    [Parameter] public bool IsExpanded { get; set; } = true;
    [Parameter] public string CurrentArea { get; set; } = "service-bus";
    [Parameter] public EventCallback<string> OnNavigate { get; set; }
}