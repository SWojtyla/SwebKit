@for (int i = 0; i < Count; i++)
{
    <div class="skeleton-row" style="width: @(GetWidth(i))%" aria-hidden="true"></div>
}

@code {
    [Parameter] public int Count { get; set; } = 6;

    private static int GetWidth(int i) => 100 - (i % 3 * 8);
}
