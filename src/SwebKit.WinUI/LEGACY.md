# LEGACY — do not modify

Abandoned WinUI experiment, kept in-repo as reference only. The primary stack is
Tauri (`src-tauri/`) + React (`web/`) + the .NET sidecar (`src-sidecar/`).

Do not search, read, or modify this project unless a task explicitly concerns
the legacy stack.
